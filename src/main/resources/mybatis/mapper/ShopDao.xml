<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.teasommelier.dao.IShopDao">

	<!-- 매장 전체 목록 조회 -->
	<select id="select_tea_shop_List" resultType="com.springboot.teasommelier.dto.ShopDto">
		select sh_no, sh_name, sh_lat, sh_lon, sh_img
		from tea_shop
		order by sh_no asc
	</select>

	<!-- 매장 상세 조회 -->
	<select id="select_tea_shop" parameterType="int" resultType="com.springboot.teasommelier.dto.ShopDto">
		select sh_no, sh_name, sh_lat, sh_lon, sh_img
		from tea_shop
		where sh_no = #{sh_no}
	</select>

	<!-- 등록 -->
	<insert id="insert_tea_shop" parameterType="com.springboot.teasommelier.dto.ShopDto">
		insert into tea_shop (sh_name, sh_lat, sh_lon, sh_img)
		values (#{sh_name}, #{sh_lat}, #{sh_lon}, #{sh_img})
	</insert>

	<!-- 수정 -->
	<update id="update_tea_shop" parameterType="com.springboot.teasommelier.dto.ShopDto">
		update tea_shop
		set sh_name = #{sh_name},
			sh_lat  = #{sh_lat},
			sh_lon  = #{sh_lon},
			sh_img  = #{sh_img}
		where sh_no = #{sh_no}
	</update>

	<!-- 삭제 -->
	<delete id="delete_tea_shop" parameterType="int">
		delete from tea_shop
		where sh_no = #{sh_no}
	</delete>

</mapper>