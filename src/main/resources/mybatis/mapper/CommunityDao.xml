<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.teasommelier.dao.ICommunityDao">

	<!-- 전체 커뮤니티 목록 조회 -->
	<select id="CommunityList" resultType="com.springboot.teasommelier.dto.CommunityDto">
		select * from tea_community order by cb_no desc
	</select>

	<!-- 카테고리별 게시글 목록 조회 -->
	<select id="CommunityList_category" resultType="com.springboot.teasommelier.dto.CommunityDto">
		select * from tea_community where cb_category=#{cb_category} order by cb_no desc
	</select>

	<!-- 게시글 상세 보기 -->
	<select id="CommunityView" resultType="com.springboot.teasommelier.dto.CommunityDto">
		select * from tea_community where cb_no=#{cb_no}
	</select>

	<!-- 조회수 1 증가 -->
	<update id="CommunityHit">
		update tea_community set cb_view_count = cb_view_count + 1 where cb_no=#{cb_no}
	</update>

	<!-- 게시글 작성 -->
	<insert id="CommunityInsert">
		insert into tea_community(
			cb_no, cb_category, cb_date, cb_subject, cb_rating, 
			cb_content, cb_view_count, cb_url, cb_file, p_no, m_no
		) values (
			tea_community_seq.NEXTVAL, #{cb_category}, SYSDATE, #{cb_subject}, #{cb_rating},
			#{cb_content}, 0, #{cb_url}, #{cb_file}, #{p_no}, #{m_no}
		)
	</insert>

	<!-- 게시글 수정 -->
	<update id="CommunityUpdate">
		update tea_community
		 set cb_subject=#{cb_subject}, cb_rating=#{cb_rating}, cb_content=#{cb_content}, cb_url=#{cb_url}, cb_file=#{cb_file}
		 where cb_no=#{cb_no}
	</update>

	<!-- 게시글 삭제 -->
	<delete id="CommunityDelete">
		delete from tea_community where cb_no=#{cb_no}
	</delete>

</mapper>